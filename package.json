{
  "name": "scattegories",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend/scattegories && npm start",
    "dev:backend": "cd backend && node server.js",
    "build": "cd frontend/scattegories && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/scattegories && npm install",
    "postinstall": "cd backend && npm install && cd ../frontend/scattegories && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
